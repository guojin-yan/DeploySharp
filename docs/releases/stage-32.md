# Stage 32: immutable GGUF admission / 阶段 32：不可变 GGUF 准入

Stage 32 closes reproducibility gaps in the Stage 31 local Qwen admission. The read-only gate now verifies GGUF magic, the exact entrypoint, all six Manifest-bound model/source files, immutable source/revision/license bindings, the model-local evidence path, evidence hash, internal model/runtime identity, and real operation semantics. Negative copies of the Manifest proved that a changed sidecar hash and a changed native runtime identity both return `BLOCKED`. / 阶段 32 补齐阶段 31 本地 Qwen 准入的可重复性缺口：只读门禁现在核对 magic、精确入口、全部六个模型/来源文件、固定来源与许可证绑定，以及 evidence 的路径、哈希、内部身份和真实操作语义；负向 Manifest 副本证明 sidecar 或 native 身份变化都会被阻断。

The LLamaSharp session now serializes concurrent disposal and disposes all owned native contexts even when an earlier disposal step fails. Runtime evidence uses create-new semantics; the retained Stage 31 file was not rewritten. The package-only consumer repeats the model SHA256 and supports a validation graph with no native package, which produced the real `DS-NATIVE-6001` diagnostic. No public API or ModelFactory surface changed. / LLamaSharp 会话现在串行化并发释放，并确保各原生资源进入释放路径；证据写入使用 create-new 语义，阶段 31 文件未被重写。纯包 consumer 重复模型哈希校验，并通过无 native 资产图验证真实 `DS-NATIVE-6001`。未新增公共 API 或 ModelFactory 能力。

Validation completed with `377 passed / 50 skipped / 0 failed` across the unfiltered solution, `8 passed / 1 skipped` for LLamaSharp without the model environment, one passing real CPU gate, and `29 passed / 2 skipped` for ModelPack. NuGet found no known direct or transitive vulnerabilities. Three related packages built, package-only skip/no-native/real paths passed, inventory remained `69/56`, and admission remained `ADMITTED missing=none`. Development packages remain unsigned and report expected `NU3004`. / 全解决方案、focused、真实 CPU、ModelPack、漏洞审计、打包、三条纯包路径、inventory 与准入均已通过；开发包仍未签名，因此签名校验按预期报告 `NU3004`。

No model, Tokenizer, sidecar, or evidence was downloaded, converted, created, or replaced. The exact Qwen remains local External with every publication flag false and outside the empty official catalog. / 本阶段没有下载、转换、创建或替换模型、Tokenizer、sidecar 或 evidence；精确 Qwen 继续保持本地 External 与全部发布门禁。
