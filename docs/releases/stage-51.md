# Stage 51: TensorRT ONNX-to-engine builder / 阶段 51：TensorRT ONNX 转 engine 构建器

The admitted net8 `JYPPX.DeploySharp.Backend.TensorRT` package now includes `TensorRtOnnxEngineBuilder`, immutable build options/results, dynamic input profiles, ONNX regular-file/SHA256 validation, atomic `.engine/.plan` output, stable `DS-TRT-5008` through `DS-TRT-5011` diagnostics, and a managed build-input hash for composition into a device-safe cache key. The inference provider remains engine-only. / 已准入的隔离 net8 包新增 ONNX builder、不可变选项/结果、动态 profile、ONNX/hash 校验、原子 engine 输出、稳定诊断码与 managed build-input hash；推理 provider 继续只接收 engine。

FP16 is limited to weakly typed TensorRT 8/10 networks. Explicit INT8 is graph-defined Q/DQ and does not enable legacy calibration. TensorRT 11 strongly typed precision is graph-defined. External-data ONNX and shape-tensor inputs are explicit first-version limitations. Native TensorRT/CUDA/cuDNN/driver/bridge and generated engines remain consumer-owned and are not packaged. / FP16 仅用于 TRT8/10 弱类型网络；显式 INT8 由 Q/DQ 图定义，不启用旧式校准；TRT11 强类型精度由图定义。external-data ONNX 与 shape-tensor 输入是首版明确限制。native graph 与生成 engine 均由 consumer 持有且不打包。

Managed contract tests pass `10/0/0` without native initialization; the full solution passes `388/50/0`. The package-only consumer references the builder surface and build-input hash without building an engine. Stage 35 passes 10 packages/83 TFMs plus 5/5 negatives; Stage 36 passes 83 API contracts, 48 managed dependencies, 83/83 SourceLink/PDB and 7/7 negatives. Real GPU build/inference remains skipped/blocked because no exact local ONNX/plan and runtime matrix were authorized. The two upstream formal-publication blockers remain retained; no TensorRT package-admission rerun or retained JSON rewrite occurred because upstream identity did not change. / managed 合同测试 `10/0/0`，全解决方案 `388/50/0`；纯包 consumer 验证 builder API。Stage 35/36 正负向门通过。真实 GPU 构建/推理因未授权精确模型和 runtime matrix 继续 skip/blocked；两项正式发布 blocker retained，上游身份未变，未重跑 package admission 或改写 retained JSON。
