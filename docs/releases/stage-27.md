# Stage 27: Document intelligence and layout understanding / 阶段 27：文档智能与版面理解

Stage 27 adds one artifact-bound LayoutLMv3/Donut/Pix2Struct family contract, typed page/layout/patch/token/schema/KV ownership, a stateful three-session generation pipeline, managed Donut tokenization and strict structured parsing, official Donut OpenCV preprocessing, ModelFactory page/schema/OCR/Processor query identity, three External manifests, a package-only consumer, acquisition guide, and a regenerated Stages 1–27 model inventory. / 阶段 27 新增统一文档模型族合同、页面/版面/Patch/Token/Schema/KV 所有权、三 Session 生成、Managed Tokenizer、严格解析、官方 OpenCV 预处理、ModelFactory 文档查询、三份 External Manifest、纯包 Consumer 与阶段 1–27 总清单。

Official Donut CORD-v2 revision `8003d433...` ran on one authorized CORD-v2 page through the official PyTorch Predictor, Python ORT/OpenVINO, DeploySharp ORT CPU, DeploySharp OpenVINO FP32 IR CPU, OpenCV, and the package-only consumer. Every path produced the same 53 completion tokens, EOS, structured fields, and JSON. This is local reproducible fidelity evidence, not a benchmark or automatic `AlgorithmVerified` promotion. / 官方 Donut 同页同 Prompt 在官方 Predictor、Python/DeploySharp 双后端、OpenCV 与纯包 Consumer 上得到完全一致的 53 Token/EOS/结构 JSON；这不是性能或自动算法认证声明。

Official LayoutLMv3 base remains blocked because it has no downstream task head, its expected official FUNSD checkpoint was unavailable, and its checkpoint license is CC-BY-NC-SA-4.0. Pix2Struct DocVQA remains blocked because no complete traceable dynamic-patch native bundle was available and its official text config disables cache. No third-party task head, invented KV, fixed JSON, or Python proxy is substituted. / LayoutLMv3 与 Pix2Struct 按精确事实保持 blocker，不使用第三方任务头、虚构 KV、固定 JSON 或 Python 代理。

The development inventory now contains 63 entries and 50 structured manifests; the unified warehouse contains 17 expected top-level directories with zero missing and zero orphan. All Stage 27 records have `redistributionAllowed:false`, `uploaded:false`, and `downloadable:false`; the embedded official catalog remains empty. Packages remain managed-only. No model upload, fake URL, commit, push, tag, Release, Actions invocation, or TensorRT implementation was performed. / 总清单现有 63 条/50 份结构化 Manifest，warehouse 17 个顶层目录且 0 缺失/0 孤儿。上传下载和 Catalog 均未变化，未执行外部发布或 TensorRT。
