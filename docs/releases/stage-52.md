# Stage 52: TensorRT CUDA/RTC managed admission / 阶段 52：TensorRT CUDA/RTC managed 准入

The isolated net8 TensorRT package now exposes an independent CUDA/RTC preprocessing/postprocessing layer over explicit caller-owned `CudaStream` and `CudaMemory`. Source/header/options and PTX/CUBIN hashes, compiler/target identity, kernel entry point, launch dimensions/shared memory, buffer type/static shape/range/access, synchronization and disposal ownership are all explicit. In-memory compilation/module load are supported; no persistent cache writer or default-stream path exists. / 隔离 net8 TensorRT 包新增基于调用方 stream/device memory 的独立 CUDA/RTC 前后处理层，显式固定源码、工件、编译器、launch、buffer、同步与释放合同；支持内存编译/加载，不提供长期 cache writer 或默认流路径。

DeploySharp owns the loaded module and returned launch owner only. The caller owns and disposes streams/buffers; the launch retains SafeHandle leases until synchronization/disposal. Inference and ONNX-builder lifecycles remain separate. Cache identity binds exact source/header/options/artifact/compiler/CUDA/driver/GPU/native-bridge fields and performs no I/O. PTX/CUBIN/fatbin/native/model/engine/plan payload remains excluded from packages, catalog, inventory, general Releases and Git. / DeploySharp 只拥有 module 与 launch owner；stream/buffer 由调用方持有，launch 在同步/释放前租用其 SafeHandle。inference/builder 生命周期保持独立；cache identity 完整绑定各字段但不写 I/O。所有 native/工件继续排除在包与发布资产外。

Focused managed tests pass `15/0/0` without native initialization. No exact authorized kernel and unique GPU/runtime matrix were supplied, so `CUDA/RTC GPU validation skipped/blocked`; no algorithm/performance claim is made. Upstream Release/tag/package identity is unchanged, blocker delta remains retained 2/new 0/disappeared 0, full package admission was not rerun, and the retained JSON was not rewritten. Formal publication remains blocked only by the immutable cross-channel manifest and same-build provenance/attestation proofs. / focused managed tests `15/0/0`；真实 GPU 验证继续 skip/blocked。上游身份未变，未重跑 package admission 或改写 retained JSON；正式发布仍只被两项 proof 阻止。

The full solution passes `393/50/0`. Stage 35 validates 10 packages/83 TFMs plus 5/5 negative scenarios; Stage 36 validates 48 managed dependencies, 4 consumer-owned native runtimes, 83 API contracts, SourceLink/PDB `83/83` plus 7/7 negative scenarios. The package-only matrix is 17 pass, 11 expected external skip and 3 expected external block. Inventory remains 69 entries/56 manifests; exact Qwen admission remains complete. NuGet reports zero vulnerable and zero deprecated packages across 20 projects; 154 outdated rows are report-only. / 全解决方案 `393/50/0`；Stage 35/36 正向及负向全部通过；纯包矩阵为 17 pass、11 expected skip、3 expected block。inventory、精确 Qwen 与依赖安全状态保持通过，outdated 只报告未升级。
