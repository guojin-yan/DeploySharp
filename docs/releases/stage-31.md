# Stage 31: authorized Qwen GGUF CPU evidence / 阶段 31：授权 Qwen GGUF CPU 实证

Stage 31 acquires the exact user-authorized Qwen2.5 0.5B Instruct Q4_K_M GGUF into `E:\DeploySharp-Models\qwen2.5-0.5b-instruct-q4_k_m`, verifies its 491,400,032-byte size and SHA256 `74a4da8c9fdbcd15bd1f6d01d621410d31c6fc00986f5eb687824e7b93d7a9db`, retains Apache-2.0 and immutable source evidence, and advances the Stage 30 gate from `READY` to `ADMITTED`. / 阶段 31 获取精确授权的 Qwen GGUF，校验大小/哈希，保留 Apache-2.0 与不可变来源证据，并将阶段 30 门禁从 `READY` 推进到 `ADMITTED`。

The LLamaSharp integration test now owns `LLamaSharp.Backend.Cpu 0.27.0` explicitly and records real CPU Generate, Stream, in-flight Cancel, deterministic Repeat, single-writer `DS-LLM-4004`, Dispose, and normalized 896-dimensional Embedding evidence. Native metadata confirms Qwen2, Q4_K Medium, context 32,768, BOS/EOS/PAD, tokenizer and embedded ChatML identity. Missing-native failures during model-parameter creation are now mapped through the existing `NativeRuntimeUnavailable` diagnostic. No public API was added. / LLamaSharp 集成测试显式持有 CPU backend，并记录完整真实 CPU 操作矩阵；原生元数据确认模型、量化、上下文与 Tokenizer 身份。模型参数创建阶段的缺原生库异常也纳入既有诊断映射；未新增公共 API。

The evidence file remains outside Git at `E:\DeploySharp-Models\qwen2.5-0.5b-instruct-q4_k_m\evidence\deploysharp-stage31-runtime.json`, 7,364 bytes, SHA256 `68f2b1e144c3d4537cb2f7c91473554296bda97a52bc5e5b5e9517dfb0dfc973`. The model remains External and local-only with `AlgorithmVerified=false`, `uploaded=false`, and `downloadable=false`; the official catalog remains empty. No commit, push, tag, Release, upload, or Actions dispatch is part of Stage 31. / 证据仅保存在模型目录；模型继续为本地 External，非 AlgorithmVerified、不可上传、不可下载，official catalog 为空。本阶段不包含任何 GitHub 发布动作。

Validation completed with `376 passed / 50 skipped / 0 failed` across the unfiltered solution, `7 passed / 1 skipped` for the LLamaSharp tests without the model environment, and one passing environment-gated real CPU test. NuGet reported no known direct or transitive vulnerabilities. Core, LLM, and Backend.LlamaSharp packages built successfully; package-only consumer skip and real CPU paths both passed. Development packages remain unsigned, so `dotnet nuget verify --all` reports the expected `NU3004`. / 全解决方案、focused、真实环境门控、漏洞审计、打包及纯包 consumer 的 skip/real 路径均已验证；开发包仍未签名，因此签名校验按预期报告 `NU3004`。
