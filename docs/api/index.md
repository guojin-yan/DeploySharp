# API Reference / API 参考

The API reference is generated from the bilingual XML comments in DeploySharp projects. / API 参考由 DeploySharp 各项目中的中英文 XML 注释生成。

`JYPPX.DeploySharp.Core` is the first V2 contract package. Additional domain and backend namespaces will appear here as their packages are introduced. / `JYPPX.DeploySharp.Core` 是首个 V2 契约包；后续领域包和后端包完成后，其命名空间也会加入此处。
