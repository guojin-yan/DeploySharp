# OpenVINO adapter fixtures

These Apache-2.0 files are generated from DeploySharp's self-owned ONNX contract graph by
`eng/test-models/Generate-OpenVinoFixtures.py` with OpenVINO `2026.2.1`. They validate the
adapter and are not official algorithm models, benchmarks, or GitHub Release assets.

这些 Apache-2.0 文件由 OpenVINO `2026.2.1` 从 DeploySharp 自有 ONNX 合同图生成，仅用于
验证适配器，不属于官方算法模型、性能基准或 GitHub Release 资产。
