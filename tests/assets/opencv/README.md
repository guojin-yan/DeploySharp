# OpenCV adapter fixtures

These tiny RGB, grayscale, and alpha PNG files are deterministic Apache-2.0 contract fixtures generated by `eng/test-models/Generate-OpenCvFixtures.ps1`. They are not official algorithm assets and are not part of the ModelFactory catalog or a GitHub Release.