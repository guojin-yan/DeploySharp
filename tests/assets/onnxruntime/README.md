# ONNX Runtime contract fixtures / ONNX Runtime 合同夹具

These tiny graphs are generated by `eng/test-models/Generate-OnnxRuntimeFixtures.py` with the versions pinned in `requirements-onnxruntime-fixtures.txt`. Run the generator from any directory; it resolves the repository root from its own path. / 这些微型计算图由 `eng/test-models/Generate-OnnxRuntimeFixtures.py` 使用 `requirements-onnxruntime-fixtures.txt` 中锁定的版本生成。可从任意目录运行脚本，脚本会根据自身路径解析仓库根目录。

The files are DeploySharp-owned Apache-2.0 test fixtures. They prove adapter contracts only and are not supported algorithm models, catalog entries, Release assets, or representative performance workloads. `fixtures.json` records exact size and SHA256 values. / 这些文件是 DeploySharp 自有的 Apache-2.0 测试夹具，仅用于验证适配器合同，不是受支持算法模型、目录条目、Release 资产或性能工作负载。`fixtures.json` 记录精确大小与 SHA256。
