# ONNX Runtime missing-native consumer / ONNX Runtime 缺失原生库消费者

This isolated executable intentionally omits every native runtime package and verifies `DS-NATIVE-6001` instead of a process crash. / 此隔离可执行程序有意不安装任何原生运行时包，并验证返回 `DS-NATIVE-6001`，而不是导致进程崩溃。
