# DeploySharp OpenCV DNN clean consumer

This package-only consumer loads a DeploySharp-owned ONNX vision fixture through the caller-selected OpenCV Windows runtime and executes a named NCHW float32 CPU inference. / 此纯包 consumer 使用调用方选择的 OpenCV Windows 运行时加载 DeploySharp 自有 ONNX 视觉夹具，并执行命名 NCHW float32 CPU 推理。
