# OpenVINO clean consumer

This package-only consumer proves that the application explicitly selects `OpenVINO.runtime.win` while the DeploySharp backend remains managed-only.

该仅包消费者证明应用显式选择 `OpenVINO.runtime.win`，DeploySharp 后端本身仍然只包含托管资产。
