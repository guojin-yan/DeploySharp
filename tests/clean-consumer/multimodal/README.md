# DeploySharp Multimodal clean consumer

This package-only consumer verifies ordered media, completed generation, streaming termination, and transitive package dependencies without repository project references. / 此纯包 consumer 校验有序媒体、完整生成、流式终止和传递包依赖，不使用仓库项目引用。
