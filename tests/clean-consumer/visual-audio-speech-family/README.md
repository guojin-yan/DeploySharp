# Visual audio speech family consumer

This consumer references only the published Core, Visual, Visual.OpenCV, and ONNX Runtime packages plus application-owned native/runtime packages. Set `DEPLOYSHARP_AUDIO_MODEL_ROOT` to the external `wav2vec2-base-960h` warehouse directory to run the real CTC task; without those files it emits the stable skip marker.
